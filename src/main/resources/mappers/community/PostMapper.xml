<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jamjam.bookjeok.domains.community.repository.mapper.PostMapper">

<select id="findAll" resultType="PostListDTO">
SELECT
      a.post_id
    , a.writer_uid
    , a.title
    , b.nickname
    FROM posts a
    JOIN members b ON (a.writer_uid = b.member_uid)
    WHERE a.is_deleted = false
</select>

    <select id="findByPostId" resultType="PostDTO">
        SELECT
          a.post_id
        , a.writer_uid
        , a.title
        , a.content
        , b.nickname
        , a.created_at
        FROM posts a
        JOIN members b ON (a.writer_uid = b.member_uid)
        WHERE a.is_deleted = false AND a.post_id = #{postId}
    </select>

</mapper>